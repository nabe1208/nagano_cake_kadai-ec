<h1>商品詳細</h1>

<!--画像があるか確認-->
<% if @item.image.attached? %>
 <%= image_tag @item.image, size: "200x200" %>
<% else %>
 <%= image_tag 'cake', size: "200x200" %>
<% end %>


<p>商品名</p>
<%= @item.name %>
<p>商品説明</p>
<%= @item.introduction %>
<p>ジャンル</p>
<%= @genre.name %>
<p>税込価格</br>
  (税抜価格)</p>
  <%= @item.price %> 円
<p>販売ステータス</p>

<p><%= link_to '編集する', edit_admin_item_path(@item.id) %></p>
